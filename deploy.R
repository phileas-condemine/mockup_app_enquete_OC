rsconnect::deployApp(account = "drees",
                     appName = "Formulaire_Enquete_OC",
                     forceUpdate = T,
                     appFileManifest = "manifest.txt")
rsconnect::showLogs(streaming = T)
